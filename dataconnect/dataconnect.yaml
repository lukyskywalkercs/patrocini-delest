specVersion: "v1beta"
serviceId: "webpatrocini"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webpatrocini-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
